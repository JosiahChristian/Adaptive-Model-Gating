# Experiment 048 — Frozen Exact 1% Sign E-Variable Confirmation

Prospectively frozen before any Experiment-048 outcomes.

The operative scientific contract is GitHub issue #130. Experiment 048 preserves Experiment 047 covariance-matched six-direction discovery and changes only the held-out confirmation e-variable.

For each of the same ten disjoint held-out candidate responses, the confirmation factor is exactly 2 for a positive response, 0 for a negative response, and 1 only for an exact zero. The terminal product is accepted at the unchanged `E >= 100` boundary. Under continuous responses this accepts exactly 10/10 positive signs, whose exact fair-sign tail is 1/1024; the >=9-positive tail is 11/1024 > 0.01. No magnitude scale or fitted betting fraction is used.

The sample split, immutable Experiment-047 discovery candidate, full-five-round latency, Experiment-031 causal context rule, Experiment-032 composition, inherited triad veto/fallback, costs, probe schedule, and calibration constants remain unchanged.

Evaluation uses Gaussian, Laplace, Student-t3, and contaminated-Gaussian diagnostic noise; gain 0.50/0.425 x noise scale 1.00/1.50; 16 cells; seeds 48000..48999; audit 48000..48004; bootstrap seed 48048.

Frozen H1-H15 and the interpretation rule are exactly those in issue #130. No split, discovery score, sign rule, or threshold may be tuned from Experiment-048 outcomes.
