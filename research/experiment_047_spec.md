# Experiment 047 — Frozen Covariance-Matched Six-Direction Discovery

Prospectively frozen before any Experiment-047 outcomes.

The operative scientific contract is GitHub issue #125. Experiment 047 changes only the discovery selector from Experiment 046: it uses all six directed discovery-only responses with the inherited topology templates and the exact scale-free covariance geometry induced by the frozen 2/5-target + 2/4-baseline discovery split.

The sample split, held-out confirmation responses, symmetry factor `1 + sgn(x) * tanh(|x| / 0.05)`, terminal `E >= 100` acceptance boundary, full-five-round latency, Experiment-031 causal context rule, Experiment-032 composition, inherited triad veto/fallback, costs, probe schedule, and calibration constants remain unchanged.

Evaluation uses Gaussian, Laplace, Student-t3, and contaminated-Gaussian diagnostic noise; gain 0.50/0.425 x noise scale 1.00/1.50; 16 cells; seeds 47000..47999; audit 47000..47004; bootstrap seed 47047.

Frozen H1-H15 and the interpretation rule are exactly those in issue #125. No selector, split, betting function, or threshold may be tuned from Experiment-047 outcomes.
